name: Build and Deploy

on:
  push:
    branches:
      - main
    # Only trigger if content or source files change
    paths:
      - 'content/**'
      - 'src/**'
      - 'scripts/**'
      - '.github/workflows/deploy.yml'

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⬢
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build Site 🏗️
        run: |
          node scripts/build.js

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
          branch: gh-pages
